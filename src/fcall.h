#ifdef FCALC_H
#define FCALC_H

#include "grammars.h" 
#include <string>

template <typename T>
class FunctionCall {
  public:
     T parse(const std::string &s) 
     std::string generate(const Message &m) 
};





#endif /* fcalc */
